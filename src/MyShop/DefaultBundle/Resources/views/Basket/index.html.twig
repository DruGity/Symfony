<div>
    {% if order.products|length > 0 %}
        <ul>
    {% for product in order.products %}
        <li><b>{{ product.model }}</b> {{ product.price }}x{{ product.count }} = {{ product.sum }}</li>
    {% endfor %}

        </ul>
        <a href="{{ path('myshop.order_confirm') }}">Оформить заказ</a>
    {% endif %}
</div>